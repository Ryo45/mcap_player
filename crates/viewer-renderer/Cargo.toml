[package]
name = "viewer-renderer"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
image.workspace = true
wgpu.workspace = true

[dev-dependencies]
viewer-core = { path = "../viewer-core" }
